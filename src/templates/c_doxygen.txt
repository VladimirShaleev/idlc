## if config.add_doc and is_inline == false and length(docs) > 0
## set max_len = 0
## for doc in docs
## set name_len = length(doc.name)
## if name_len > max_len
## set max_len = name_len
## endif
## endfor
{{ indents }}/**
## for doc in docs
{{ indents }} * @{{doc.name}} {{ indents(max_len - length(doc.name)) }}{% for literal in doc.literals %}{% set str=cliteral(literal) %}{{ str }}{% if str == "\n" %}{{ indents }} *   {{ indents(max_len) }}{% endif %}{% endfor %}
## endfor
## if exists("license")
{{ indents }} *{{ nl(1) }}{{ indents }} *     {% for literal in license %}{% set str=cliteral(literal) %}{{ str }}{% if str == "\n" %}{{ indents }} *     {% endif %}{% endfor %}
## endif
{{ indents }} */
## endif